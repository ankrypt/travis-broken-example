language: php
php:
  - 5.5
  - 5.4
  - hhvm
before_install:
  - git clone --recursive https://www.github.com/in-toto/in-toto.git
  - pwd
  - ls -a
  - cd in-toto 
install:
  - pwd
  - ls -a
  - sudo -H pip install foo 
script: phpunit Test.php
