language: php
php:
  - 5.5
  - 5.4
  - hhvm
before_install:
  - git clone --recursive https://www.github.com/in-toto/in-toto.git
  - pwd
  - ls /in-toto/
install:
  - pip install -e . 
script: phpunit Test.php
